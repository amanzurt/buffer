import { NextResponse } from "next/server";

// Placeholder — implementado en Plan B (Instagram OAuth)
export async function GET() {
  return NextResponse.json({ message: "Meta OAuth callback — Plan B" }, { status: 501 });
}
